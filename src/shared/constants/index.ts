export enum EUserType {
  ADMIN = 'AD',
  PET_SISTER = 'PS',
  PET_OWNER = 'PO',
}

export enum EStatuz {
  VERIFIED = 'Verified',
  CLOSED = 'Closed',
  NOT_VERIFIED = 'Not verified',
  REQUEST_FOR_VERIFICATION = 'Request for verification',
  IN_VERIFYING = 'In verifying',
  TERMINATED = 'Terminated',
}
